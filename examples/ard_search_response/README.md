# ARD v0.91 SearchResponse

`search-response-v0.91.json` exercises the pinned current wire shape, including a
registry referral. Validate it without making a network request:

```bash
ardguard validate --kind ard examples/ard_search_response/search-response-v0.91.json
```
